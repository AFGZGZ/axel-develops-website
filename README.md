Typescript + React + Vite + Tailwind + Motion + three.js (with fiber & drei)

For deployment in Github Pages:

After commiting the changes to the deploy branch use 'git subtree push --prefix dist origin deploy'
