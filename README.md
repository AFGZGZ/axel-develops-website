Typescript + React + Vite + Tailwind + Motion + three.js (with fiber & drei)

For deployment in Github Pages:

After creating a new build and commiting the changes to the deploy branch use 'git subtree push --prefix dist origin deploy' to push the only the dist to the online branch.
